const passengerModel = require('./passenger.model');
const travelModel = require('./travel.model');
const driversModel = require('./driver.model');
const carsModel = require('./car.model');

module.exports = {
  passengerModel,
  travelModel,
  driversModel,
  carsModel,
};